{
  "framework": "vite",
  "buildCommand": "npm run build && cp -r admin dist/admin",
  "outputDirectory": "dist",
  "routes": [
    { "src": "^/admin/?$", "dest": "/admin/login.html" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ]
}


